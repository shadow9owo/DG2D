#include <iostream>
#include <cstring>
#include <array>
#include <limits>
#include <vector>

namespace DG2D
{
    extern std::vector<int> ConvertCharArrayToIntArray(const char value[INT16_MAX], size_t& outLength);
}